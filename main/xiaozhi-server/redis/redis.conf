# 启用 RDB 持久化
save 900 1
save 300 10
save 60 10000

# RDB 文件名
dbfilename dump.rdb

# 工作目录（必须可写，对应挂载的 /data）
dir /data

# 关闭 AOF（默认就是关闭，显式声明更安全）
appendonly no

# 允许远程连接（Docker 场景通常需要）
bind 0.0.0.0

# 不启用保护模式（避免无法从宿主机连接）
protected-mode no

# 默认端口（容器内保持 6379，由 Docker 映射）
port 6379
